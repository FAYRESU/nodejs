const name = 'Natthakan Jamratphum';
const hello = (name) => `Hello ${name}`;
const sum  = (a, b) => a + b;
const result = sum(2, 3);
// console.log("result", result, hello(name));
module.exports = sum;